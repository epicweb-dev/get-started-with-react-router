import { createRoot } from 'react-dom/client'
// 🐨 import the RouterProvider from react-router
import './styles/index.css'
// 🐨 import the router from the routes.tsx file

// 🐨 render the RouterProvider with the router
createRoot(document.getElementById('🧭')!).render(
	<div>TODO: render the RouterProvider with the router</div>,
)
